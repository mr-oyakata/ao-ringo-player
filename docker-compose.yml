# version: '3'
services:
  app:
    # image: node:lts
    build: .
    volumes:
      - ./workspace:/workspace
      - ./docs:/workspace/dist
      - /workspace/node_modules
    working_dir: /workspace
    ports:
      - 5173:5173
    tty: true
